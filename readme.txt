npm init //create the node server
create the index.js file
install cheerio to scrape web pages
install express js
install axios

initialize the index.js file
    setup the server port (8000 or 3000)
    import express / cheerio and axios
    initiating and calling the express function
    call and setup the app in console log to listen and check

change the script in package.json for the  
"scripts": {
    "start":"nodemon index.js"

